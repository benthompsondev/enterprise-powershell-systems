name: PowerShell demo checks

on:
  push:
  pull_request:

jobs:
  learner-onboarding-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run learner onboarding demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-healthcare-systems/learner-onboarding-automation"
          ./tests/Run-DemoCheck.ps1

  password-remediation-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run password remediation demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-healthcare-systems/password-remediation-workflow"
          ./tests/Run-DemoCheck.ps1

  browser-bookmark-migration-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run browser bookmark migration demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-endpoint-systems/browser-bookmark-migration"
          ./tests/Run-DemoCheck.ps1

  workforce-platform-identity-migration-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run workforce platform identity migration demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-identity-systems/workforce-platform-identity-migration"
          ./tests/Run-DemoCheck.ps1

  workstation-migration-state-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run workstation migration state demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-endpoint-systems/workstation-migration-state-toolkit"
          ./tests/Run-DemoCheck.ps1

  o365-migration-readiness-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run O365 migration readiness demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-messaging-systems/o365-migration-readiness-toolkit"
          ./tests/Run-DemoCheck.ps1

  support-and-code-review-utilities-demo:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run support and code review utilities demo check
        shell: pwsh
        run: |
          Set-Location "enterprise-support-systems/enterprise-support-and-code-review-utilities"
          ./tests/Run-DemoCheck.ps1
